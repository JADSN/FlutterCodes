import 'package:flutter/material.dart';

class FcRecordsPage extends StatelessWidget {
  const FcRecordsPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Registros'),
      ),
      body: Container(),
    );
  }
}
